import Link from "next/link";
import { getOwnerPayments } from "@/features/bookings/queries";
import { formatManilaDateTime,formatMoney } from "@/lib/format";
export const metadata={title:"Payment review"};export const instant=false;
type Props={searchParams:Promise<{group?:string;q?:string}>};
const groups=["new","overdue","approved","rejected"] as const;
export default async function PaymentsPage({searchParams}:Props){const query=await searchParams;const group=groups.includes(query.group as never)?query.group as typeof groups[number]:"new";const search=query.q??"";const items=await getOwnerPayments({group,search});return <div><p className="text-sm font-semibold text-brand-800">Owner queue</p><h1 className="text-h1 mt-1">Payment review</h1><p className="mt-3 max-w-xl text-ink-muted">Match the GCash sender, amount, and claim time before deciding.</p><nav className="mt-6 flex gap-2 overflow-x-auto" aria-label="Payment groups">{groups.map(value=><Link key={value} href={`/portal/owner/payments?group=${value}`} className={`min-h-11 rounded-xl px-4 py-3 text-sm font-semibold capitalize ${group===value?"bg-brand-950 text-white":"border border-line bg-surface"}`}>{value}</Link>)}</nav><form className="mt-4 flex gap-2"><input name="q" defaultValue={search} aria-label="Search payments" placeholder="Search code, customer, sender, phone, reference" className="min-h-11 min-w-0 flex-1 rounded-xl border border-line bg-surface px-3"/><input type="hidden" name="group" value={group}/><button className="min-h-11 rounded-xl bg-brand-900 px-4 text-sm font-semibold text-white">Search</button></form><div className="mt-5 overflow-hidden rounded-2xl border border-line bg-surface">{items.length?items.map(item=><Link key={item.id} href={`/portal/owner/bookings/${item.bookingId}`} className="grid gap-3 border-b border-line p-4 last:border-0 hover:bg-brand-50 sm:grid-cols-[1fr_auto]"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{item.booking.customerName}</p>{item.overdueAt&&<span className="rounded-lg bg-danger-soft px-2 py-1 text-xs font-semibold text-danger">Overdue</span>}</div><p className="tabular mt-1 text-xs text-ink-muted">{item.booking.publicCode} · {formatManilaDateTime(item.booking.requestedStartsAt)}</p><p className="mt-2 text-sm">Sender: <strong>{item.senderName??item.booking.gcashSenderName}</strong>{item.paymentReference?` · Ref ${item.paymentReference}`:""}</p><p className="mt-1 text-xs text-ink-muted">Claimed {item.claimedAt?formatManilaDateTime(item.claimedAt):"—"}</p></div><div className="sm:text-right"><p className="tabular font-semibold text-brand-900">{formatMoney(item.expectedCentavos)}</p><p className="mt-1 text-xs font-semibold text-ink-muted">{item.status}</p></div></Link>):<div className="p-6"><h2 className="font-semibold">No matching payments</h2><p className="mt-2 text-sm text-ink-muted">Claims and decisions will appear here.</p></div>}</div></div>}
