"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { CalendarBlank, House, UserCircle } from "@phosphor-icons/react";
const items=[{href:"/portal/staff/today",label:"Today",Icon:House},{href:"/portal/staff/schedule",label:"Schedule",Icon:CalendarBlank},{href:"/portal/staff/account",label:"Account",Icon:UserCircle}];
export function StaffNav(){const path=usePathname();return <><nav aria-label="Staff workspace" className="hidden w-64 shrink-0 border-r border-line bg-surface p-4 lg:block"><p className="px-3 pb-3 text-xs font-semibold text-ink-subtle">Staff workspace</p><ul className="space-y-1">{items.map(({href,label,Icon})=>{const active=path.startsWith(href);return <li key={href}><Link href={href} aria-current={active?"page":undefined} className={`flex min-h-11 items-center gap-3 rounded-xl px-3 text-sm font-semibold ${active?"bg-brand-900 text-white":"text-ink-muted hover:bg-brand-50"}`}><Icon aria-hidden size={19}/>{label}</Link></li>})}</ul></nav><nav className="safe-bottom fixed inset-x-0 bottom-0 z-20 border-t border-line bg-surface/95 px-2 pt-2 backdrop-blur-sm lg:hidden" aria-label="Staff navigation"><ul className="mx-auto grid max-w-sm grid-cols-3">{items.map(({href,label,Icon})=>{const active=path.startsWith(href);return <li key={href}><Link href={href} aria-current={active?"page":undefined} className={`flex min-h-13 flex-col items-center justify-center gap-1 rounded-xl text-[11px] font-semibold ${active?"text-brand-900":"text-ink-subtle"}`}><Icon aria-hidden size={20} weight={active?"fill":"regular"}/>{label}</Link></li>})}</ul></nav></>}
